apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "idv.fullname" . }}-backoffice
  labels:
    {{- include "idv.backoffice.labels" . | nindent 4 }}
spec:
  {{- if not .Values.backoffice.autoscaling.enabled }}
  replicas: {{ .Values.backoffice.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "idv.backoffice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configs/idv-config.yaml") . | sha256sum }}
        checksum/version: {{ .Values.versionSha | quote }}
        {{- if .Values.podAnnotations }}
        {{- toYaml .Values.podAnnotations | nindent 8 }}
        {{- end }}
      labels:
        {{- include "idv.backoffice.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "idv.serviceAccountName" . }}
      {{- with .Values.backoffice.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.backoffice.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.backoffice.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.backoffice.topologySpreadConstraints }}
      topologySpreadConstraints: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
      {{- if .Values.licenseSecretName }}
      - name: license
        secret:
          secretName: {{ .Values.licenseSecretName }}
      {{- end }}
      - name: idv-config
        configMap:
          name: {{ include "idv.fullname" . }}-config
          items:
            - key: idv-config
              path: idv-config.yaml
      {{- if and (hasKey .Values.config.storage "gcs") .Values.config.storage.gcs.gcsKeyJsonSecretName }}
      - name: gcs-credentials
        secret:
          secretName: {{ template "idv.gcs.credentials.secret" . }}
      {{- end }}
      {{- if or .Values.tls.trustedCABundle.configMapName .Values.extraVolumes }}
      {{- include "idv.extraVolumes" . | nindent 6 }}
      {{- end }}
      securityContext: {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.backoffice.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.backoffice.terminationGracePeriodSeconds }}
      {{- end }}
      {{- include "idv.minioInitContainer" . | nindent 6 }}
      containers:
      - name: backoffice
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        resources: {{- toYaml .Values.backoffice.resources | nindent 10 }}
        securityContext: {{- toYaml .Values.securityContext | nindent 10 }}
        {{- if .Values.backoffice.probes.livenessProbe.enabled }}
        livenessProbe:
          httpGet:
            path: /api/health
            port: api
          initialDelaySeconds: {{ .Values.backoffice.probes.livenessProbe.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.backoffice.probes.livenessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.backoffice.probes.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.backoffice.probes.livenessProbe.failureThreshold }}
          successThreshold: {{ .Values.backoffice.probes.livenessProbe.successThreshold }}
        {{- end }}
        {{- if .Values.backoffice.probes.readinessProbe.enabled }}
        readinessProbe:
          httpGet:
            path: /api/health
            port: api
          initialDelaySeconds: {{ .Values.backoffice.probes.readinessProbe.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.backoffice.probes.readinessProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.backoffice.probes.readinessProbe.periodSeconds }}
          failureThreshold: {{ .Values.backoffice.probes.readinessProbe.failureThreshold }}
          successThreshold: {{ .Values.backoffice.probes.readinessProbe.successThreshold }}
        {{- end }}
        {{- if .Values.backoffice.probes.startupProbe.enabled }}
        startupProbe:
          httpGet:
            path: /api/health
            port: api
          initialDelaySeconds: {{ .Values.backoffice.probes.startupProbe.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.backoffice.probes.startupProbe.timeoutSeconds }}
          periodSeconds: {{ .Values.backoffice.probes.startupProbe.periodSeconds }}
          failureThreshold: {{ .Values.backoffice.probes.startupProbe.failureThreshold }}
          successThreshold: {{ .Values.backoffice.probes.startupProbe.successThreshold }}
        {{- end }}
        lifecycle: {{- toYaml .Values.backoffice.lifecycle | nindent 10 }}
        ports:
        - name: api
          containerPort: {{ .Values.config.services.api.port }}
          protocol: TCP
        volumeMounts:
        {{- if .Values.licenseSecretName }}
        - name: license
          mountPath: /app/extBin/unix/regula.license
          subPath: regula.license
          readOnly: true
        {{- end }}
        - name: idv-config
          mountPath: /app/config.yaml
          subPath: idv-config.yaml
        {{- if and (hasKey .Values.config.storage "gcs") .Values.config.storage.gcs.gcsKeyJsonSecretName }}
        - name: gcs-credentials
          mountPath: /etc/credentials/gcs_key.json
          subPath: gcs_key.json
          readOnly: true
        {{- end }}
        {{- if or .Values.tls.trustedCABundle.configMapName .Values.extraVolumeMounts }}
        {{- include "idv.extraVolumeMounts" . | nindent 8 }}
        {{- end }}
        command: ["idv"]
        args: ["webserver", "start"] # [TODO] Old style, change to "idv backoffice start" later
        {{- if .Values.tls.trustedCABundle.configMapName }}
        env: {{- tpl (include "idv.fullEnv" .) $ | nindent 10 }}
        {{- else }}
        env: {{- tpl (toYaml .Values.env) . | nindent 10 }}
        {{- end }}
