{{- if and .Values.config.services.frontoffice.enabled .Values.frontoffice.autoscaling.enabled .Values.frontoffice.autoscaling.keda.enabled .Values.frontoffice.autoscaling.keda.TriggerAuthentication }}
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: {{ include "idv.fullname" . }}-frontoffice
  labels:
    {{- include "idv.labels" . | nindent 4 }}
spec:
  {{- if .Values.frontoffice.autoscaling.keda.TriggerAuthentication.secretTargetRef }}
  secretTargetRef:
  {{- range .Values.frontoffice.autoscaling.keda.TriggerAuthentication.secretTargetRef }}
  - parameter: {{ .parameter }}
    name: {{ .name }}
    key: {{ .key }}
  {{- end }}
  {{- end }}
  {{- if .Values.frontoffice.autoscaling.keda.TriggerAuthentication.podIdentity }}
  podIdentity:
    provider: {{ .Values.frontoffice.autoscaling.keda.TriggerAuthentication.podIdentity.provider }}
  {{- end }}
{{- end }}
